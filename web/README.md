# Glow Dental — web app

The active Next.js application. Full setup, roles, env vars, and handoff notes live in the repo root:

→ **[../README.md](../README.md)**

```bash
npm install
cp .env.example .env   # fill in keys
npx prisma migrate deploy
npm run dev
```
